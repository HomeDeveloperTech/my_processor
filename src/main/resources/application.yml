spring:
  application:
    name: FicoProcessor
  profiles:
    active: dev
  datasource:
    hikari:
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
      validation-timeout: 5000
management:
  endpoints:
    web:
      exposure:
        include: health,info
